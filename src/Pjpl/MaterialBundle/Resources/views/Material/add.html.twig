{% extends 'base.html.twig' %}

{% block body %}
	<h3>Dodawanie materiału</h3>
{#	<a href="{{path('homepage')}}"> Strona główna</a>#}
	<a href="{{path('material_material')}}"> Zarządzanie materiałami</a>
	<hr>
	{#
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
#}
{% endblock %}